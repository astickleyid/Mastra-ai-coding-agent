{
  "name": "personal-desktop-sandbox",
  "version": "1.0.0",
  "description": "A personal desktop sandbox with a coding agent inside.",
  "main": "dist/main.js",
  "scripts": {
    "start": "npm run build:electron && electron .",
    "build": "npm run build:electron && electron-builder",
    "build:electron": "npx tsc -p tsconfig.electron.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "engines": {
    "node": ">=20.9.0"
  },
  "dependencies": {
    "@ai-sdk/google": "latest",
    "@e2b/code-interpreter": "^1.5.1",
    "ai": "^5.0.38",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "electron": "^38.1.0",
    "electron-builder": "^26.0.12",
    "esbuild": "^0.25.9",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
